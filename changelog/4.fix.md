- Replaced `distutils.dir_util.copy_tree` with `shutil.copytree(..., dirs_exist_ok=True)` in the Fortran CICERO-SCM adapter so the package can be imported on Python 3.12 (`distutils` was removed in 3.12). Fixed an `int()` call on a non-0-dimensional numpy array in the FaIR 1.6 emissions translation that became an error under numpy 2.x. Behaviour is unchanged on supported earlier versions. Note that full Python 3.12 support is not yet declared in the package classifiers because of a separate upstream scmdata/numpy 2.x compatibility issue.
